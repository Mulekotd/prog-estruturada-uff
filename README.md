# Prog Estruturada UFF

Repositório para a disciplina de Programação Estruturada onde tem a resolução de problemas, estudo de algorítmos e estruturas de dados.

# Build

Exemplo de compilação de código utilizando o GCC.

```bash
gcc -Wall exercises/decisao_verificar_triangulo.c -o build/decisao_verificar_triangulo

gcc src/BinaryTree.c tests/TestBinaryTree.c -Iinclude -o build/binary_tree
```
